# Archive — unused components

These components are **not used** on the live site. They’re kept here so the main folders only show what’s in use. Safe to delete if you don’t need them.

- **ContactSection** — never imported
- **HeadingLink** — never imported
- **TableOfContents** — not used on the about page
